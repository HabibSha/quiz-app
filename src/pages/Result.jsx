import styles from "../styles";

const Result = () => {
  return (
    <section>
      <article className={`${styles.container} ${styles.paddingY}`}>
        <h2>result</h2>
      </article>
    </section>
  );
};

export default Result;
